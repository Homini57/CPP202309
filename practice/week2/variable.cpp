#include <iostream>

using namespace std;

int main()
{
	// 정수 변수 one, two, sum을 선언하고
	// one과 two를 각각 1, 2로 초기화합니다.
	int one = 1;
	int two = 2;
	//one과 two의 초기화된 값을 합하여 sum을 초기화합니다.
	int sum = one + two;

	//sum의 초기화된 값을 콘솔창에 출력합니다.
	cout << "1 + 2 = " << sum << endl;

	return 0;
}